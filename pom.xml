<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>top.terapeak</groupId>
  <artifactId>janitor</artifactId>
  <version>1.0.0</version>
  <packaging>pom</packaging>

  <name>Janitor</name>
  <description>Multi-framework JPA entity cleanup library for Spring Boot, Quarkus and KumuluzEE</description>
  <url>https://github.com/terapeak-top/janitor</url>

  <modules>
    <module>janitor-core</module>
    <module>janitor-spring-boot</module>
    <!--    <module>janitor-quarkus</module>-->
    <module>janitor-kumuluzee</module>
  </modules>

  <licenses>
    <license>
      <name>The Apache License, Version 2.0</name>
      <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
    </license>
  </licenses>

  <developers>
    <developer>
      <name>Ilija Trajkoski</name>
      <email>ilija.trajkoski@gmail.com</email>
      <organization>Terapeak Ilija Trajkoski s.p.</organization>
      <organizationUrl>http://www.terapeak.top</organizationUrl>
    </developer>
  </developers>
  <scm>
    <connection>scm:git:git://github.com/terapeak-top/janitor.git</connection>
    <developerConnection>scm:git:ssh://github.com/terapeak-top/janitor.git</developerConnection>
    <url>https://github.com/terapeak-top/janitor</url>
  </scm>

  <properties>
    <java.version>11</java.version>
    <maven.compiler.source>${java.version}</maven.compiler.source>
    <maven.compiler.target>${java.version}</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Dependency versions -->
    <jakarta.persistence.version>3.1.0</jakarta.persistence.version>
    <jakarta.inject.version>2.0.1</jakarta.inject.version>
    <jakarta.annotation.version>2.1.1</jakarta.annotation.version>

    <spring.boot.version>3.2.0</spring.boot.version>
    <quarkus.version>3.6.0</quarkus.version>
    <kumuluzee.version>4.1.0</kumuluzee.version>

    <slf4j.version>2.0.9</slf4j.version>
    <junit.version>5.10.1</junit.version>
    <mockito.version>5.8.0</mockito.version>
    <auto-service.version>1.1.1</auto-service.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Internal modules -->
      <dependency>
        <groupId>top.terapeak</groupId>
        <artifactId>janitor-core</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>top.terapeak</groupId>
        <artifactId>janitor-quarkus-runtime</artifactId>
        <version>${project.version}</version>
      </dependency>

      <!-- Jakarta EE -->
      <dependency>
        <groupId>jakarta.persistence</groupId>
        <artifactId>jakarta.persistence-api</artifactId>
        <version>${jakarta.persistence.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.inject</groupId>
        <artifactId>jakarta.inject-api</artifactId>
        <version>${jakarta.inject.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.annotation</groupId>
        <artifactId>jakarta.annotation-api</artifactId>
        <version>${jakarta.annotation.version}</version>
      </dependency>

      <!-- Spring Boot BOM -->
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring.boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- Quarkus BOM -->
      <dependency>
        <groupId>io.quarkus</groupId>
        <artifactId>quarkus-bom</artifactId>
        <version>${quarkus.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- KumuluzEE BOM -->
      <dependency>
        <groupId>com.kumuluz.ee</groupId>
        <artifactId>kumuluzee-bom</artifactId>
        <version>${kumuluzee.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- Logging -->
      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-api</artifactId>
        <version>${slf4j.version}</version>
      </dependency>

      <!-- Auto-service (annotation processor helper) -->
      <dependency>
        <groupId>com.google.auto.service</groupId>
        <artifactId>auto-service</artifactId>
        <version>${auto-service.version}</version>
      </dependency>

      <!-- Test -->
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>${junit.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-core</artifactId>
        <version>${mockito.version}</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.12.1</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.3</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-gpg-plugin</artifactId>
        <executions>
          <execution>
            <id>sign-artifacts</id>
            <phase>verify</phase>
            <goals>
              <goal>sign</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.sonatype.central</groupId>
        <artifactId>central-publishing-maven-plugin</artifactId>
        <version>0.9.0</version>
        <extensions>true</extensions>
      </plugin>
    </plugins>
  </build>
</project>
